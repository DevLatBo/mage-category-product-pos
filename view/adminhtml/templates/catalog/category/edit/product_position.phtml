<pre>
categoryId: <?= (int)($block->getCategoryId() ?? 0) ?>
hasCategory: <?= $block->getCategory() ? 'YES' : 'NO' ?>
</pre>


<?php
$productsCollection = $block->getProductsCollection();

echo "<pre>";print_r($productsCollection->getData());echo "</pre>";
?>

<div class="admin-product-pos-list">
    <?php foreach ($productsCollection as $product): ?>
        <div class="admin-product-card">
            <img src="<?= $block->getProductImageUrl($product); ?>" width="80" />
            <div>
                <strong><?= $block->escapeHtml($product->getName()) ?></strong><br>
                SKU: <?= $block->escapeHtml($product->getSku()) ?><br>
                Pos: <?= (int)$product->getData('position') ?>
            </div>
        </div>
    <?php endforeach; ?>
</div>

